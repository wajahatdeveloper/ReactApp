import { useNavigate } from 'react-router-dom';

export const Login = () => {
    const navigate = useNavigate();

    return (
        <div>
            <h1>Login Title</h1>
            <h1>Login Form</h1>
        </div>
    );
};